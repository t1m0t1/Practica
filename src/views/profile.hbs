<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card text-center">
                <div class="card-body">
                    <h3>Bienvenido {{user.apellido}} {{{user.nombre}}} </h3>
                    <h4>{{user.username}}</h4>
                    <a href="/inicio" class="btn btn-primary m-4">
                        Ingresar    
                    </a>
                </div>
            </div>
        </div>

    </div>
</div>
